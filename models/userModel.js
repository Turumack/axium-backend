// Este archivo está preparado por si decides separar lógica de base de datos.
// Actualmente no se usa directamente porque se trabaja desde el controlador.

module.exports = {
    table: 'users'
  };
  